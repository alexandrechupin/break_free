<div class="d-flex align-items-center justify-content-center file-banner pt-3 pb-3">
  <p class="circle mb-0">!</p>
  <span class="pl-3">Votre dossier n’est complet qu’à 20%. Certains actions requièrent un dossier complet afin que nous puissions vous aider au mieux.</span>
</div>

<div class="container mt-5 pb-3">

  <h1>Mon dossier</h1>

  <div class="mt-5">

    <span><%= render 'file-box' %></span>

  </div>

  <div class="mt-5">

    <h2>Ce que vous pouvez faire</h2>
    <% @incident.incident_category.recommendations.each do |recommendation| %>
      <% if recommendation.is_an_action %>
        <div class="card-action ml-0 mt-3">
          <h5>📩  <%= recommendation.title %></h5>
          <i class="fas fa-arrow-circle-right"></i>
        </div>
      <% end %>
    <% end %>
  </div>
</div>

